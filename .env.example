# ============================================
# SENSITIVE - Keep these values secret!
# ============================================

# Database password - Change this to a strong password in production
# Generate with: openssl rand -base64 24
DB_PASSWORD=changeme

# Database connection string - Contains DB_PASSWORD (sensitive)
DATABASE_URL=postgresql://painchain:${DB_PASSWORD}@db:5432/painchain

# ============================================
# Note: Non-sensitive config (REDIS_URL, VITE_API_URL)
# is hardcoded in docker-compose.yml
# ============================================
