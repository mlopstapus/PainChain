apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "painchain.fullname" . }}
  labels:
    {{- include "painchain.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "painchain.fullname" . }}
subjects:
- kind: ServiceAccount
  name: {{ include "painchain.fullname" . }}
  namespace: {{ .Release.Namespace }}
